import { View, Text } from 'react-native';
import React from 'react';

interface INotifCardProps {}
const NotifCard = ({}: INotifCardProps) => {
  return (
    <View>
      <Text>NotifCard</Text>
    </View>
  );
};

export default NotifCard;
